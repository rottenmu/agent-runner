{
  "mcpServers": {
    "agent-memory": {
      "type": "http",
      "url": "http://localhost:9900/api/agent-memory/mcp",
      "headers": {
        "Authorization": "Bearer ${AGENT_MEMORY_TOKEN}"
      },
      "description": "智能体四层记忆（H2 OLTP + Arrow OLAP）：memory_write / memory_read / memory_delete / memory_search"
    }
  }
}
